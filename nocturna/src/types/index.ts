export interface CharacterInput {
    left: boolean;
    right: boolean;
    jump: boolean;
}

export interface Position {
    x: number;
    y: number;
}

export interface Velocity {
    x: number;
    y: number;
}